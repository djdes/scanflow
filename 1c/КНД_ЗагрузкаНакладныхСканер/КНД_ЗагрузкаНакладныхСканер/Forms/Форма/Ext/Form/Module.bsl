
&НаСервере
Функция ЗагрузитьНакладныеНаСервере()
	ОбъектОбработка = РеквизитФормыВЗначение("Объект");
	Возврат ОбъектОбработка.ЗагрузитьНакладныеСоСканера();
КонецФункции

&НаКлиенте
Процедура ЗагрузитьНакладные(Команда)
	Отчёт = ЗагрузитьНакладныеНаСервере();
	// Показываем результат в модальном окне — чтобы пользователь точно его увидел,
	// даже если окно сообщений скрыто. Параллельно отчёт уже попал в Журнал
	// регистрации (см. СоздатьОтчётСтроку в модуле объекта).
	ПоказатьПредупреждение(, Отчёт, 30);
КонецПроцедуры
