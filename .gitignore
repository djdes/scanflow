node_modules/
dist/
data/database.sqlite
data/inbox/*
data/processed/*
data/failed/*
!data/inbox/.gitkeep
!data/processed/.gitkeep
!data/failed/.gitkeep
.env
google-credentials.json

# Invoice dumps & debug crops (supplier PII / bank details / real invoice fragments).
# Recursive: catches data/backup_*.json, data/*.png crops, data/reupload-backup/*.jpeg, etc.
# Only the .gitkeep placeholders below stay tracked.
data/backup_*.json
data/**/*.json
data/**/*.png
data/**/*.jpg
data/**/*.jpeg
data/**/*.JPG
data/**/*.JPEG
data/*.png
data/*.jpg
data/*.jpeg
data/*.JPG
data/*.JPEG

# Sber TLS certificates (server-only) — [Cc] so case-sensitive FS (Linux/CI/WSL) also ignores Certs/
[Cc]erts/*
![Cc]erts/.gitkeep
DocsApiSber/

# 1C compiled external processing
1c/**/*.epf

# Screenshots / scratch images dumped at repo root
/*.png
/*.jpeg
/*.jpg

# Scratch photo/catalog dumps (real invoice photos)
tmp-photos/

*.log
logs/
logs/.*-audit.json

# Dev artifacts
.claude/
.playwright-mcp/
.vscode/
nul
eng.traineddata
rus.traineddata
temp_invoice.json
cwww1C-JPGExchangetemp_invoice.json
temp_*.json
temp_*.txt

# Backups (server-only)
data/backups/
datainbox.gitkeep
architecture.excalidraw
improvements.excalidraw

# Legacy SQLite leftovers (DB is now MariaDB; these are kept for one-shot migration)
data/database.sqlite-wal
data/database.sqlite-shm
data/legacy-database.sqlite
data/database.sqlite.malformed-*

# Tesseract trained data
osd.traineddata

# Local Obsidian / generated views
_view/

# Git worktrees (used by subagent-driven development)
.worktrees/

# subagent-driven-development scratch (ledger, reports, review packages)
.superpowers/
