node_modules/
dist/
data/database.sqlite
data/inbox/*
data/processed/*
data/failed/*
!data/inbox/.gitkeep
!data/processed/.gitkeep
!data/failed/.gitkeep
.env
google-credentials.json

# Sber TLS certificates (server-only)
certs/*
!certs/.gitkeep
DocsApiSber/

*.log
logs/
logs/.*-audit.json

# Dev artifacts
.claude/
.playwright-mcp/
.vscode/
nul
eng.traineddata
rus.traineddata
temp_invoice.json
cwww1C-JPGExchangetemp_invoice.json
temp_*.json
temp_*.txt

# Backups (server-only)
data/backups/
datainbox.gitkeep
architecture.excalidraw
improvements.excalidraw

# Legacy SQLite leftovers (DB is now MariaDB; these are kept for one-shot migration)
data/database.sqlite-wal
data/database.sqlite-shm
data/legacy-database.sqlite
data/database.sqlite.malformed-*

# Tesseract trained data
osd.traineddata

# Local Obsidian / generated views
_view/

# Git worktrees (used by subagent-driven development)
.worktrees/

# subagent-driven-development scratch (ledger, reports, review packages)
.superpowers/

# Локальные вспомогательные скрипты (не часть продукта) — дважды случайно
# попадали в коммит через "git add src".
src/scripts/fill-mappings.py
src/scripts/generate-mappings.ts
src/scripts/ingest-photo.ts
src/scripts/test-claude-direct.ts

# 1С external processings — бинарь, пересобирается из 1c/.../Ext/*.bsl + формы
*.epf
